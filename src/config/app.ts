/** App metadata — version is injected at build time from package.json */
export const APP_VERSION = __APP_VERSION__
export const IS_BETA = true
export const SUPPORT_EMAIL = 'collectiveactionsupport@gmail.com'
